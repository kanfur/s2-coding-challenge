version: '3.8'

services:
  mysql:
    image: mysql/mysql-server:latest
    restart: on-failure
    volumes:
      - "dbdata:/var/lib/mysql"
    environment:
      MYSQL_ROOT_PASSWORD: ${DATABASE_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DATABASE_NAME}
      MYSQL_USER: ${DATABASE_USER}
      MYSQL_PASSWORD: ${DATABASE_PASSWORD}
#      MYSQL_TCP_PORT: 3307
    ports:
      - 3307:3307
#    expose:
#      - 3307

  php:
    build: ./docker/php/
    restart: on-failure
    depends_on:
      - mysql
    environment:
      - DATABASE_URL=mysql://${DATABASE_USER}:${DATABASE_PASSWORD}@mysql:3307/${DATABASE_NAME}?serverVersion=8.0
    links:
      - mysql
    volumes:
      - ".:/var/www"

  nginx:
    build: ./docker/nginx/
    restart: on-failure
    depends_on:
      - php
    volumes:
      - ".:/var/www"
    ports:
      - 80:80

volumes:
  dbdata:
